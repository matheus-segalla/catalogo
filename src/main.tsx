import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css'; // <== ESSENCIAL para aplicar os estilos Tailwind

ReactDOM.createRoot(document.getElementById('root')!).render(
  <App />
);
